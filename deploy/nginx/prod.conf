server {
    listen 80;
    server_name _;
    
    # 1. Frontend (Default Root)
    root /var/www/frontend/dist;
    index index.html index.php;

    client_max_body_size 100M;

    # 2. Backend API Routing
    location /api {
        try_files $uri $uri/ @laravel;
    }
    
    location /sanctum {
        try_files $uri $uri/ @laravel;
    }

    location /up {
        try_files $uri $uri/ @laravel;
    }

    # Storage (Images)
    location /storage {
        alias /var/www/backend/public/storage;
    }

    # 3. Laravel Bridge
    location @laravel {
        root /var/www/backend/public;
        rewrite ^ /index.php last;
    }

    # 4. PHP Execution (Docker Service: app:9000)
    location ~ \.php$ {
        root /var/www/backend/public;
        include fastcgi_params;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        
        # CRITICAL FIX: Map Nginx path (/var/www/backend/...) to PHP Container path (/var/www/...)
        fastcgi_param SCRIPT_FILENAME /var/www/public$fastcgi_script_name;
    }

    # 5. React Router (Frontend Fallback)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
